apiVersion: 1

providers:
  - name: RecipeRec
    orgId: 1
    folder: RecipeRec
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: ${GRAFANA_DASHBOARDS_PATH}
